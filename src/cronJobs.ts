// import cron from 'node-cron';
// import dayjs from 'dayjs';

// import updateGlobalCacheState from './validation/validateGlobalCacheState';

// const previousDay = dayjs().subtract(1, 'day');

// const cronJobs = [];

// export const instantiateCronJobs = () => {
//   cronJobs.forEach((cronJobFn) => cronJobFn());
// };
